-- config for hangover

-- globally default station
default_station = "oslobass"

-- file path for tracks
tracks_path = "/srv/mu"

-- upload staging area.
-- must be on same device as tracks_path, or error: "invalid cross-device link"
temp_dir = "/srv/tmp"

